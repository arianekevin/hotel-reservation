/**
 * Contains Spring Data JPA repositories — interfaces that handle database access.
 * No business logic here. Only data access (SELECT, INSERT, UPDATE, DELETE).
 */
package com.hotel.repository;