spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hotel
    username: hotel_user
    password: hotel_pass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

server:
  port: 8080